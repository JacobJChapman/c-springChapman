#include <iostream>

using namespace std;

void addTax(float r, float c;) {
  value = c + (c * r);
}

int main() {
  float value;
  float cost;
  float rate;
  cout << "Enter tax rate %:" >>
  cin >> rate;
  rate = rate / 100.0;
  cout << rate << "%" << endl;
  cout << "Enter the cost in $:" << endl;
  cin >> cost;
  addTax(rate, cost);
  cout << "$" << value << " is the total pice." << endl;

}
//Unable to figure out the errors.
